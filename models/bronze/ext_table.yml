version: 2

sources:
  - name: default
    tables:
      - name: raw
        description: "orders data stored as CSV files in ADLS"
        external:
          location: 'dbfs:/mnt/cntdlt/bronze/c4c/'
          using: json
          options:
            inferschema: 'true'